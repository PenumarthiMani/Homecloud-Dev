<aura:component implements="flexipage:availableForRecordHome,force:hasRecordId,force:lightningQuickActionWithoutHeader" access="global" controller="StandardQuoteController">
    <aura:attribute name="Spinner" type="boolean" default="false"/>
    <aura:attribute name="recordId" type="String"/> 
    <aura:attribute name="QuoteRecordType" type="String"/> 
    <aura:attribute name="today" type="Date" default="" />
     <aura:attribute name="isError" type="Boolean" default="false" />
    <aura:handler name="init" value="{!this}" action="{!c.doInit}"/>
    <aura:attribute name="connection" type="Object"/>
    <aura:attribute name="simpleconnection" type="Object"/>
    <!--loading spinner start... style=Brand Medium (blue dots)-->
    <aura:if isTrue="{!v.Spinner}">
        <div aura:id="spinnerId" class="slds-spinner_container">
            <div class="slds-spinner--brand  slds-spinner slds-spinner--large slds-is-relative" role="alert">
                <span class="slds-assistive-text">Loading</span>
                <div class="slds-spinner__dot-a"></div>
                <div class="slds-spinner__dot-b"></div>
            </div>
        </div>
    </aura:if>
    <force:recordData aura:id="connectionRecordLoader"
                      recordId="{!v.recordId}"
                      fields="Name,Division__c,Community__c,Homebuyer__c,Estimated_Close_Date__c"
                      targetRecord="{!v.connection}"
                      targetFields="{!v.simpleconnection}"
                      recordUpdated="{!c.load}"/>
    <lightning:card class="slds-text-heading_small"  title="New Scenario">
        <h3 class="slds-section-title--divider">Scenario Information</h3>
        <div class="slds-p-around_medium">
            <lightning:recordEditForm aura:id="editForm" objectApiName="Scenario__c" recordTypeId="{!v.QuoteRecordType}" onsubmit="{!c.onSubmit}" onsuccess="{!c.handleSuccess}" onerror="{!c.errorInformation}">
                <lightning:messages />
                <lightning:inputField fieldName="Connection__c" value="{!v.simpleconnection.Id}" disabled="true" />
                <lightning:inputField fieldName="Division__c" value="{!v.simpleconnection.Division__c}" disabled="true" />
                <lightning:inputField fieldName="Community__c" value="{!v.simpleconnection.Community__c}" disabled="true" />
				<lightning:inputField fieldName="Homebuyer__c" value="{!v.simpleconnection.Homebuyer__c}" disabled="true" />
                <lightning:inputField fieldName="Status__c" disabled="true" />
                    <div class="slds-size--1-of-1">
                      <!--  <lightning:inputField fieldName="Target_Closing_Date__c" value="{!v.simpleconnection.Estimated_Close_Date__c}"/>-->
                        <ui:inputDate aura:id="datefield" value="{!v.today}" label="Target Closing Date:" displayDatePicker="true" format="MM/dd/yyyy" required="true" />   
                    </div>
                <lightning:layout horizontalAlign="center" class="slds-m-top_large">
                    <lightning:button variant="neutral" label="Cancel" title="Cancel" type="text" onclick="{!c.handleCancel}"/>
                    <lightning:button variant="brand" label="Create" title="Create" type="submit" disabled="{!v.Spinner}"/>
                </lightning:layout>
            </lightning:recordEditForm>
        </div>
    </lightning:card>
</aura:component>